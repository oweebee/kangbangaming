services:
  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      - STEAM_API_KEY=${STEAM_API_KEY}
      - STEAM_ID=${STEAM_ID}
      - PORT=3001
    volumes:
      - kanban_data:/app/data

  frontend:
    build: ./frontend
    restart: unless-stopped
    depends_on:
      - backend

volumes:
  kanban_data:
